# SkinCareProductRecommendationSystem
🧴 AI-powered skincare product recommendation system using ensemble ML (Gradient Boosting + Random Forest + Logistic Regression + Bagging) — personalized by skin type.
